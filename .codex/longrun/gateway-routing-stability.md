# 网关调用稳定性长跑状态

## 任务
- 标题：网关调用稳定性与 `gpt-5.4` 选路异常排查
- 目标：确认线上为何未持续复用已成功站点，并修复仍存在的错误选路/不重试问题
- 范围：`tokenRouter`、`proxyRetryPolicy`、相关持久化状态与线上生产验证
- 完成定义：
  - 复盘线上 `gpt-5.4` 最近成功后仍切站失败的真实原因
  - 若有逻辑缺陷，完成代码修复、测试、部署
  - 生产验证确认成功站点复用与跨站重试符合规则

## 里程碑
1. 已验证：确认当前部署版本与容器健康
2. 进行中：核对生产数据库中的 `gpt-5.4` 请求链路、成功记忆、熔断状态
3. 未开始：定位代码层面的具体缺陷或边界条件
4. 未开始：完成修复与回归测试
5. 未开始：部署正式环境并复检生产日志

## 最近结论
- 当前代码头部提交为 `e8f5496`
- 线上容器正常，本地健康检查 `200`
- 外网 `/sites` 偶发 TLS EOF，暂未见应用侧故障证据
- 本轮重点不是健康检查，而是确认成功站点为何未被继续复用

## 下一动作
- 直接查询生产 MySQL 中 `proxy_logs`、模型级运行时健康状态/熔断状态
- 对照 `tokenRouter` 中最近成功站点复用与模型成功站点复用逻辑
